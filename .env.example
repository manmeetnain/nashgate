# Copy to .env and fill in — matches the api_key_env names in
# docs/example.config.yaml. If you use your own config, update these
# to match its backends instead.
NASHGATE_BACKEND_FAST_KEY=
NASHGATE_BACKEND_MID_KEY=
NASHGATE_BACKEND_LOCAL_KEY=
